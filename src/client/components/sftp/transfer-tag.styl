.transfer-tag
  border-radius 7px
  padding 1px 7px
  font-weight 700
  background rgba(19, 36, 47, .06)
  border 1px solid var(--ios-border)
.transfer-status-init
  color var(--primary)
.transfer-status-started
  color var(--success)
.transfer-status-error
  color var(--error)
